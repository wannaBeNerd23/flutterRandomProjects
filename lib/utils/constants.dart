import 'package:flutter/material.dart';

class Constants {
  static const standardAnimationDuration = 500;
  static const double fabButtonSize = 64;
  static const Color primaryColor = Color(0xff0055FF);

  static const fabCrossIconAnimationDuration = 200;
  static const consultationListPageScreenTransitionDuration = 2000;
  static const fabPadding = EdgeInsets.only(
    left: 20,
    top: 60,
    right: 30,
    bottom: 0,
  );
}
